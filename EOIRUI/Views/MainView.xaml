<Window x:Class="EOIRUI.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="EO/IR UDP Console"
        Height="640"
        Width="960"
        MinHeight="520"
        MinWidth="760"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock FontSize="24"
                       FontWeight="SemiBold"
                       Text="EO/IR UDP Console" />
            <Border Grid.Column="1"
                    Padding="10,5"
                    Background="#FFE8EEF7"
                    CornerRadius="4">
                <TextBlock Text="{Binding StatusMessage}" />
            </Border>
        </Grid>

        <GroupBox Grid.Row="1" Header="UDP 설정" Margin="0,0,0,12">
            <Grid Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="수신 포트" />
                <TextBox Grid.Column="1"
                         Margin="8,0,0,0"
                         Padding="6,4"
                         IsEnabled="{Binding IsNotListening}"
                         Text="{Binding LocalPort, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Column="3" VerticalAlignment="Center" Text="송신 주소" />
                <TextBox Grid.Column="4"
                         Margin="8,0,0,0"
                         Padding="6,4"
                         Text="{Binding RemoteHost, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Column="6" VerticalAlignment="Center" Text="송신 포트" />
                <TextBox Grid.Column="7"
                         Margin="8,0,0,0"
                         Padding="6,4"
                         Text="{Binding RemotePort, UpdateSourceTrigger=PropertyChanged}" />

                <Button Grid.Column="9"
                        MinWidth="72"
                        Padding="10,5"
                        Command="{Binding StartCommand}"
                        Content="열기" />
                <Button Grid.Column="10"
                        MinWidth="72"
                        Margin="8,0,0,0"
                        Padding="10,5"
                        Command="{Binding StopCommand}"
                        Content="닫기" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="송신" Margin="0,0,0,12">
            <Grid Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Padding="8,5"
                         Text="{Binding OutgoingMessage, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="1"
                        MinWidth="80"
                        Margin="8,0,0,0"
                        Padding="12,5"
                        Command="{Binding SendCommand}"
                        Content="보내기" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="송수신 로그">
            <Grid Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DataGrid AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          ItemsSource="{Binding LogEntries}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="100" Binding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}" Header="시간" />
                        <DataGridTextColumn Width="60" Binding="{Binding Direction}" Header="구분" />
                        <DataGridTextColumn Width="170" Binding="{Binding EndPoint}" Header="Endpoint" />
                        <DataGridTextColumn Width="*" Binding="{Binding Message}" Header="내용" />
                    </DataGrid.Columns>
                </DataGrid>

                <Button Grid.Row="1"
                        HorizontalAlignment="Right"
                        Margin="0,8,0,0"
                        Padding="12,5"
                        Command="{Binding ClearLogCommand}"
                        Content="로그 지우기" />
            </Grid>
        </GroupBox>
    </Grid>
</Window>
